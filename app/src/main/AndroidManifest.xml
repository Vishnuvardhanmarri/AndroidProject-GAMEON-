<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vishnu.projectwork">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Signup"
            android:label="@string/title_activity_signup"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".welcome"
            android:label="@string/title_activity_welcome"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".video"
            android:label="@string/title_activity_video"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".platforms"
            android:label="@string/title_activity_platforms"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".xbox"
            android:label="@string/title_activity_xbox"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".playstation"
            android:label="@string/title_activity_playstation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".otheraccessories"
            android:label="@string/title_activity_otheraccessories"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".gamecds"
            android:label="@string/title_activity_gamecds"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".screen"
            android:label="screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".tabs"
            android:label="@string/title_activity_tabs"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".about_us" />
        <activity android:name=".contact_us" />
        <activity android:name=".xboxwelcome" />
        <activity android:name=".purchasehere" />
        <activity android:name=".gamesrequired" />
        <activity android:name=".splash1" />
        <activity android:name=".splash2" />
        <activity android:name=".cod" />
        <activity android:name=".moredetailed"></activity>
    </application>

</manifest>